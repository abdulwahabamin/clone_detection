        private void updateSummary(String key, boolean changing) {
            switch (key) {
                case Constants.KEY_PREF_LOCATION_GPS_ENABLED:
                    entrySummary(key);
                    break;
            }
        }

